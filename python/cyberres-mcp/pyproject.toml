#
# Copyright contributors to the agentic-ai-cyberres project
#
[project]
name = "cyberres-mcp"
version = "0.1.0"
description = "MCP server for validating recovered infrastructure resources including Linux VMs, Oracle databases, and MongoDB clusters. Provides tools to check network connectivity, OS health, database connectivity, and replica status with secure credential handling and structured logging."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.122.1",
    "fastmcp>=2.14.0",
    "mcp[cli]>=1.23.1",
    "starlette>=0.49.1",
    "oracledb>=3.3.0",
    "paramiko>=3.5.1",
    "cryptography>=46.0.5",
    "pynacl>=1.6.2",
    "authlib>=1.6.6",
    "pydantic>=2.11.7",
    "pymongo>=4.14.1",
    "requests>=2.32.4",
    "urllib3>=2.6.3",
    "python-multipart>=0.0.22",
    "uvicorn>=0.35.0",
    "python-dotenv>=1.0.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
cyberres-mcp = "cyberres_mcp.server:main"
