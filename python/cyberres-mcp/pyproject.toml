#
# Copyright contributors to the agentic-ai-cyberres project
#
[project]
name = "cyberres-mcp"
version = "0.1.0"
description = "MCP server for validating recovered infrastructure resources including Linux VMs, Oracle databases, and MongoDB clusters. Provides tools to check network connectivity, OS health, database connectivity, and replica status with secure credential handling and structured logging."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.1.0",
    "mcp[cli]>=1.23.1",
    "paramiko>=3.5.1",
    "cryptography>=46.0.5",
    "pynacl>=1.6.2",
    "pydantic>=2.11.7",
    "requests>=2.32.4",
    "urllib3>=2.6.3",
    "uvicorn>=0.35.0",
    "python-dotenv>=1.0.1",
]

[project.optional-dependencies]
oracle = ["oracledb>=3.3.0"]
mongo = ["pymongo>=4.14.1"]
all = ["cyberres-mcp[oracle,mongo]"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
cyberres-mcp = "cyberres_mcp.server:main"
