{
  "_comment": "Secrets file example — copy to secrets.json and fill in real credentials. secrets.json is gitignored.",
  "_schema": "Host-keyed: the top-level key is the IP address or hostname of the target.",

  "192.168.1.100": {
    "ssh": {
      "username": "root",
      "password": "CHANGE_ME",
      "key_path": "/home/admin/.ssh/id_rsa"
    }
  },

  "prod-vm-01": {
    "ssh": {
      "username": "root",
      "password": "CHANGE_ME"
    }
  },

  "db.example.com": {
    "ssh": {
      "username": "root",
      "password": "CHANGE_ME"
    },
    "oracle": {
      "username": "system",
      "password": "CHANGE_ME",
      "service": "ORCL",
      "port": 1521
    }
  },

  "mongo.example.com": {
    "ssh": {
      "username": "admin",
      "password": "CHANGE_ME"
    },
    "mongo": {
      "username": "mongo_admin",
      "password": "CHANGE_ME",
      "auth_db": "admin",
      "port": 27017
    }
  }
}